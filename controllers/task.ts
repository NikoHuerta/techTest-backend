import { Request, Response } from 'express';

import { Task } from '../models';

export const getTasks = async (req: Request, res: Response) => {
    //this is the controller method that handles the GET (all) request
};